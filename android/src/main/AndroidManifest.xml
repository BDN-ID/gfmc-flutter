<?xml version="1.0" encoding="utf-8"?>
<!--
  Deliberately empty of components: GfmcHubActivity, the :gfmc process
  services, and the manifest-merged permissions all come from the
  com.sltr.gfmc:gfmc-sdk AAR dependency (see build.gradle) and merge into the
  host app automatically. Redeclaring any of that here would just risk a
  manifest merge conflict with the real definitions.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
